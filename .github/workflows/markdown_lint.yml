name: Markdown Lint

on:
  pull_request:
  push:
    branches: [main]

jobs:
  rumdl-check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - uses: rvben/rumdl@v0.1.53
        with:
          path: .
          args: --exclude CHANGELOG.md
